#!/bin/sh

linenum=0

store()
{
while read line
do
lines[$linenum]="$line"
linenum=$(expr "$linenum" + 1)
done
}

flow()
{
curline=0

while [ "$curline" -lt "$linenum" ]
do
echo ${lines[$curline]}
curline=$(expr "$curline" + 1)
done
}
